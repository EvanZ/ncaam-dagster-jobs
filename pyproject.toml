[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dagster]
module_name = "dagster_jobs"

[tool.pytest.ini_options]
filterwarnings = [
    "ignore::DeprecationWarning:google._upb._message.ScalarMapContainer"
]
